services:
  - type: web
    name: leo-web
    runtime: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: LEO_WEATHER_LOCATION
        value: Lezhë, Albania
      - key: PORT
        value: 8000
    healthCheckPath: /api/health
